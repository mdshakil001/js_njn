const para = document.querySelectorAll('p')
const err = document.querySelectorAll('.error')

para.forEach(p => {
    console.log(p);
})

console.log("================================");
err.forEach(p => {
    console.log(p);
})